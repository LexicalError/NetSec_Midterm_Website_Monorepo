{
    "scripts": {
        "vercel-build": "pip install -r backend/requirements.txt & python manage.py makemigrations --noinput & python manage.py migrate --noinput & python manage.py migrate sessions --fake-initial"
    }
}